export enum RangeType {
  day = 'day',
  week = 'week',
  month = 'month',
}

export enum Integration {
  slack,
  telegram,
}

export enum Rate {
  good = 'good',
  bad = 'bad',
  neutral = 'neutral',
}

export enum SlackMessageBlockType {
  header = 'header',
  divider = 'divider',
  section = 'section',
}
